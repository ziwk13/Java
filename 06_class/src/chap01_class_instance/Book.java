package chap01_class_instance;

public class Book {
	
	// 클래스는 값(필드)과 기능(메소드)을 가지는 코드 블록 이다.
	
	// 클래스가 가지고 있는 값(필드)과 기능(메소드)를 사용 하려면 일반적으로
	// 해당 클래스를 타입으로 가지는 객체(인스턴스)를 생성 한다.
	
	// 생성한 객체(인스턴스)를 이용 해서 값(필드)을 사용 하거나 기능(메소드)을 호출 한다. 

}
